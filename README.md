# mktools

A reusable toolkit with:

- `mktools.kstat` for dataset profiling and EDA preparation
- `mktools.kfile` for filesystem and ZIP handling
- `mktools.kio` for robust tabular-data imports
- `mktools.common` for shared runtime config and exceptions
